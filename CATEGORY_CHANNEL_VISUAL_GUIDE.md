# Category and Channel System - Visual Guide

## System Architecture

```
┌────────────────────────────────────────────────────────────────┐
│                    TSI Manager Application                      │
└────────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
  ┌──────────┐        ┌──────────┐        ┌──────────┐
  │ Frontend │        │ Backend  │        │ Database │
  │  React   │◄──────►│  API     │◄──────►│ Supabase │
  │          │        │ Express  │        │ Postgres │
  └──────────┘        └──────────┘        └──────────┘
                            │
                            │ Socket.IO
                            ▼
                      Real-time Messaging
```

---

## Database Schema Visual

```
┌─────────────────────────────────────────────────────────────┐
│                    chat_channels Table                       │
├─────────────────────────────────────────────────────────────┤
│ • id (UUID) PRIMARY KEY                                     │
│ • name (TEXT)                                               │
│ • channel_type (TEXT) ← 'category' | 'text' | 'voice'      │
│ • parent_id (UUID) → references chat_channels(id)          │
│ • visibility (TEXT) ← 'public' | 'private'                 │
│ • created_by (UUID) → references auth.users(id)            │
│ • group_id (UUID) → references study_groups(id)            │
│ • position (INTEGER)                                        │
│ • created_at (TIMESTAMP)                                    │
│ • updated_at (TIMESTAMP)                                    │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ CASCADE DELETE
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                  channel_memberships Table                   │
├─────────────────────────────────────────────────────────────┤
│ • id (UUID) PRIMARY KEY                                     │
│ • channel_id (UUID) → references chat_channels(id)         │
│ • user_id (UUID) → references auth.users(id)               │
│ • role (TEXT) ← 'owner' | 'moderator' | 'member'          │
│ • joined_at (TIMESTAMP)                                     │
│ • UNIQUE(channel_id, user_id)                              │
└─────────────────────────────────────────────────────────────┘
```

---

## Channel Hierarchy

```
📁 Study Resources (Category - Public)
    ├── 💬 General Discussion (Text - Public)
    ├── 💬 Questions & Answers (Text - Public)
    ├── 🔊 Study Voice 1 (Voice - Public)
    └── 🔒🔊 Private Study Session (Voice - Private)
         └── Members: Alice (owner), Bob (member), Carol (moderator)

📁 Mathematics (Category - Public)
    ├── 💬 Algebra Chat (Text - Public)
    ├── 💬 Calculus Help (Text - Public)
    └── 🔒💬 Exam Prep (Text - Private)
         └── Members: David (owner), Eve (member)

💬 Random Chat (Text - Public) ← Orphan channel (no category)
```

---

## User Roles & Permissions

```
┌─────────────────────────────────────────────────────────────┐
│                        OWNER                                 │
├─────────────────────────────────────────────────────────────┤
│ ✓ View channel                                              │
│ ✓ Send messages                                             │
│ ✓ Update channel (name, visibility, position)              │
│ ✓ Delete channel                                            │
│ ✓ Add members (owners, moderators, members)                │
│ ✓ Remove members (including other owners)                  │
│ ✓ Change member roles                                       │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      MODERATOR                               │
├─────────────────────────────────────────────────────────────┤
│ ✓ View channel                                              │
│ ✓ Send messages                                             │
│ ✓ Update channel (name, position)                          │
│ ✗ Delete channel                                            │
│ ✓ Add members (moderators, members)                        │
│ ✓ Remove members (except owners)                           │
│ ✓ Change member roles (except owners)                      │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                       MEMBER                                 │
├─────────────────────────────────────────────────────────────┤
│ ✓ View channel                                              │
│ ✓ Send messages                                             │
│ ✗ Update channel                                            │
│ ✗ Delete channel                                            │
│ ✗ Add members                                               │
│ ✓ Leave channel (remove self)                              │
│ ✗ Change member roles                                       │
└─────────────────────────────────────────────────────────────┘
```

---

## API Flow Diagrams

### Creating a Category with Channels

```
Client                    API Server              Database
  │                           │                       │
  │  POST /api/channels       │                       │
  │  {name: "Study",          │                       │
  │   type: "category"}       │                       │
  ├──────────────────────────►│                       │
  │                           │  INSERT category      │
  │                           ├──────────────────────►│
  │                           │  Return category data │
  │                           │◄──────────────────────┤
  │  {id: "cat-123",          │                       │
  │   name: "Study"}          │                       │
  │◄──────────────────────────┤                       │
  │                           │                       │
  │  POST /api/channels       │                       │
  │  {name: "Chat",           │                       │
  │   type: "text",           │                       │
  │   parent_id: "cat-123"}   │                       │
  ├──────────────────────────►│                       │
  │                           │  Verify category      │
  │                           ├──────────────────────►│
  │                           │◄──────────────────────┤
  │                           │  INSERT channel       │
  │                           ├──────────────────────►│
  │                           │◄──────────────────────┤
  │  {id: "ch-456",           │                       │
  │   name: "Chat",           │                       │
  │   parent_id: "cat-123"}   │                       │
  │◄──────────────────────────┤                       │
```

### Private Channel Access Control

```
Client                    API Server              Database
  │                           │                       │
  │  POST /api/channels       │                       │
  │  {visibility: "private"}  │                       │
  ├──────────────────────────►│                       │
  │                           │  INSERT channel       │
  │                           ├──────────────────────►│
  │                           │◄──────────────────────┤
  │                           │  TRIGGER: Add creator │
  │                           │  as owner             │
  │                           ├──────────────────────►│
  │                           │◄──────────────────────┤
  │  Channel created          │                       │
  │◄──────────────────────────┤                       │
  │                           │                       │
  │  POST /memberships        │                       │
  │  {target_user_id: "..."}  │                       │
  ├──────────────────────────►│                       │
  │                           │  Verify requester is  │
  │                           │  owner/moderator      │
  │                           ├──────────────────────►│
  │                           │◄──────────────────────┤
  │                           │  INSERT membership    │
  │                           ├──────────────────────►│
  │                           │◄──────────────────────┤
  │  Member added             │                       │
  │◄──────────────────────────┤                       │
```

---

## Visibility Rules

### Public Channels

```
┌─────────────────────────────────────────────────────────────┐
│                      PUBLIC CHANNEL                          │
│                   (visibility: 'public')                     │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Accessible to: ALL authenticated users                      │
│                                                              │
│  No membership required                                      │
│                                                              │
│  Anyone can:                                                 │
│    • View channel list                                       │
│    • Read messages                                           │
│    • Send messages                                           │
│                                                              │
│  Use cases:                                                  │
│    • General discussion forums                               │
│    • Public announcements                                    │
│    • Open study groups                                       │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### Private Channels

```
┌─────────────────────────────────────────────────────────────┐
│                     PRIVATE CHANNEL                          │
│                  (visibility: 'private')                     │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Accessible to: ONLY members                                 │
│                                                              │
│  Membership required with role:                              │
│    • owner                                                   │
│    • moderator                                               │
│    • member                                                  │
│                                                              │
│  Members can:                                                │
│    • View channel (if member)                                │
│    • Read messages                                           │
│    • Send messages                                           │
│    • Manage members (if owner/moderator)                     │
│                                                              │
│  Use cases:                                                  │
│    • Private study groups                                    │
│    • Team coordination                                       │
│    • Restricted discussions                                  │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## Data Flow: List Channels with Hierarchy

```
┌─────────────────────────────────────────────────────────────┐
│  GET /api/channels?user_id=USER&include_children=true        │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
        ┌──────────────────────────────────┐
        │  1. Fetch public channels         │
        │     WHERE visibility = 'public'   │
        │     AND group_id IS NULL          │
        └──────────────────────────────────┘
                           │
                           ▼
        ┌──────────────────────────────────┐
        │  2. Fetch private channels        │
        │     JOIN channel_memberships     │
        │     WHERE visibility = 'private'  │
        │     AND user_id = USER            │
        └──────────────────────────────────┘
                           │
                           ▼
        ┌──────────────────────────────────┐
        │  3. Combine results               │
        │     public + private              │
        └──────────────────────────────────┘
                           │
                           ▼
        ┌──────────────────────────────────┐
        │  4. Organize by hierarchy         │
        │     - Filter categories           │
        │     - Filter channels             │
        │     - Match parent_id             │
        └──────────────────────────────────┘
                           │
                           ▼
        ┌──────────────────────────────────┐
        │  5. Return structured response    │
        │     {                             │
        │       categories: [...],          │
        │       orphan_channels: [...]      │
        │     }                             │
        └──────────────────────────────────┘
```

---

## Security Flow

```
┌─────────────────────────────────────────────────────────────┐
│                      Request Arrives                         │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
        ┌──────────────────────────────────┐
        │  1. Authenticate User             │
        │     ✓ user_id provided?           │
        │     ✗ → 401 Unauthorized          │
        └──────────────────────────────────┘
                           │
                           ▼
        ┌──────────────────────────────────┐
        │  2. Validate Input                │
        │     ✓ All required fields?        │
        │     ✓ Correct types?              │
        │     ✓ Length limits?              │
        │     ✗ → 400 Bad Request           │
        └──────────────────────────────────┘
                           │
                           ▼
        ┌──────────────────────────────────┐
        │  3. Check Permissions             │
        │     For private channels:         │
        │     ✓ Is user a member?           │
        │     ✓ Does user have role?        │
        │     ✗ → 403 Forbidden             │
        └──────────────────────────────────┘
                           │
                           ▼
        ┌──────────────────────────────────┐
        │  4. Verify Constraints            │
        │     ✓ Category has no parent?     │
        │     ✓ Parent is category?         │
        │     ✓ References exist?           │
        │     ✗ → 400 Bad Request           │
        └──────────────────────────────────┘
                           │
                           ▼
        ┌──────────────────────────────────┐
        │  5. Execute Database Query        │
        │     Via Supabase (SQL safe)       │
        │     With RLS policies             │
        └──────────────────────────────────┘
                           │
                           ▼
        ┌──────────────────────────────────┐
        │  6. Return Response               │
        │     200/201 Success               │
        │     or error code                 │
        └──────────────────────────────────┘
```

---

## Usage Scenarios

### Scenario 1: Student Creates a Study Group

```
1. Alice creates a "Calculus Study" category
   POST /api/channels
   {name: "Calculus Study", type: "category", visibility: "public"}

2. Alice adds a public discussion channel
   POST /api/channels
   {name: "Questions", type: "text", parent_id: "...", visibility: "public"}

3. Alice creates a private voice channel for her group
   POST /api/channels
   {name: "Study Session", type: "voice", parent_id: "...", visibility: "private"}
   → Automatically becomes owner

4. Alice invites Bob and Carol
   POST /api/channels/{id}/memberships
   {target_user_id: "bob-id", role: "member"}
   {target_user_id: "carol-id", role: "moderator"}

5. Everyone can now join the voice channel
```

### Scenario 2: Teacher Manages Class Channels

```
1. Teacher creates "Math Class" category
   POST /api/channels
   {name: "Math Class", type: "category", visibility: "public"}

2. Creates public announcement channel
   POST /api/channels
   {name: "Announcements", type: "text", parent_id: "...", visibility: "public"}

3. Creates private grading discussion
   POST /api/channels
   {name: "Grading", type: "text", parent_id: "...", visibility: "private"}
   → Only teacher and TAs have access

4. Adds TAs as moderators
   POST /api/channels/{id}/memberships
   {target_user_id: "ta1-id", role: "moderator"}
   {target_user_id: "ta2-id", role: "moderator"}
```

### Scenario 3: Removing Access

```
1. User wants to leave a channel
   DELETE /api/channels/{id}/memberships/{their-user-id}?user_id={their-user-id}
   → Self-removal always allowed

2. Owner removes a disruptive member
   DELETE /api/channels/{id}/memberships/{member-id}?user_id={owner-id}
   → Owner can remove anyone

3. Moderator tries to remove owner
   DELETE /api/channels/{id}/memberships/{owner-id}?user_id={mod-id}
   → Rejected: 403 Forbidden
```

---

## Testing Checklist

### ✅ Database Tests
- [ ] Migration runs successfully
- [ ] All tables created correctly
- [ ] Indexes created
- [ ] RLS policies active
- [ ] Triggers functioning
- [ ] Constraints enforced

### ✅ API Tests
- [ ] Create category (valid)
- [ ] Create text channel (valid)
- [ ] Create voice channel (valid)
- [ ] Create private channel → auto-membership
- [ ] Reject category with parent
- [ ] Reject invalid channel type
- [ ] Reject channel with non-category parent
- [ ] List channels with hierarchy
- [ ] Update channel name
- [ ] Update channel visibility
- [ ] Delete channel (owner only)
- [ ] Reject delete category with children
- [ ] Add member to private channel
- [ ] Reject add member to public channel
- [ ] List channel members
- [ ] Remove member (owner)
- [ ] Reject moderator removing owner
- [ ] Self-removal from channel

### ✅ Security Tests
- [ ] CodeQL passes (0 vulnerabilities)
- [ ] RLS blocks unauthorized access
- [ ] Input validation works
- [ ] SQL injection prevented
- [ ] Length limits enforced
- [ ] Type validation works
- [ ] Role checks enforced

---

## Monitoring & Maintenance

### Key Metrics to Track

```
┌─────────────────────────────────────────────────────────────┐
│  Performance Metrics                                         │
├─────────────────────────────────────────────────────────────┤
│  • API response time per endpoint                            │
│  • Database query execution time                             │
│  • Number of active channels                                 │
│  • Number of memberships                                     │
│  • Message throughput                                        │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  Usage Metrics                                               │
├─────────────────────────────────────────────────────────────┤
│  • Categories created per day                                │
│  • Channels created per day                                  │
│  • Public vs private ratio                                   │
│  • Active users per channel                                  │
│  • Membership changes per day                                │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  Error Metrics                                               │
├─────────────────────────────────────────────────────────────┤
│  • 4xx errors (client errors)                                │
│  • 5xx errors (server errors)                                │
│  • Database connection errors                                │
│  • RLS policy violations                                     │
│  • Failed membership operations                              │
└─────────────────────────────────────────────────────────────┘
```

---

## Quick Reference

### Channel Types
- `category` - Container for channels
- `text` - Text-based chat
- `voice` - Voice communication

### Visibility
- `public` - Everyone can access
- `private` - Members only

### Roles
- `owner` - Full control
- `moderator` - Manage members
- `member` - Basic access

### Key Endpoints
- POST `/api/channels` - Create
- GET `/api/channels` - List
- GET `/api/channels/:id` - Get details
- GET `/api/channels/:id/children` - Get children
- PUT `/api/channels/:id` - Update
- DELETE `/api/channels/:id` - Delete
- POST `/api/channels/:id/memberships` - Add member
- GET `/api/channels/:id/memberships` - List members
- DELETE `/api/channels/:id/memberships/:userId` - Remove member

---

**Legend:**
- 📁 = Category
- 💬 = Text Channel
- 🔊 = Voice Channel
- 🔒 = Private
- ✓ = Allowed
- ✗ = Not Allowed
